{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cadenza.dev/contracts/actor-mutation-commit-request-v1.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "contract",
    "version",
    "actor_mutation_key",
    "authority",
    "execution",
    "expected_state_version",
    "next_state",
    "next_state_digest",
    "result",
    "result_digest",
    "requested_at_unix_ms"
  ],
  "properties": {
    "contract": { "const": "cadenza.actor-mutation-commit-request" },
    "version": { "const": "1.0.0" },
    "actor_mutation_key": { "$ref": "#/$defs/key" },
    "authority": { "$ref": "actor-residency-grant-v1.schema.json" },
    "execution": { "$ref": "#/$defs/execution" },
    "expected_state_version": { "$ref": "#/$defs/nonnegative" },
    "next_state": { "type": "object" },
    "next_state_digest": { "$ref": "#/$defs/digest" },
    "result": {},
    "result_digest": { "$ref": "#/$defs/digest" },
    "requested_at_unix_ms": { "$ref": "#/$defs/positive" }
  },
  "$defs": {
    "key": {
      "type": "string",
      "minLength": 1,
      "maxLength": 512,
      "pattern": "^[a-z0-9]+(?:[.-][a-z0-9]+)*$"
    },
    "digest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
    "positive": {
      "type": "integer",
      "minimum": 1,
      "maximum": 9007199254740991
    },
    "nonnegative": {
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991
    },
    "execution": {
      "type": "object",
      "additionalProperties": false,
      "required": ["trace_key", "graph_execution_key", "task_execution_key"],
      "properties": {
        "trace_key": { "type": "string", "pattern": "^trace\\.[a-f0-9-]{36}$" },
        "graph_execution_key": { "type": "string", "pattern": "^graph-execution\\.[a-f0-9-]{36}$" },
        "task_execution_key": { "type": "string", "pattern": "^task-execution\\.[a-f0-9-]{36}$" }
      }
    }
  }
}

# cadenza-engine

Role: reserved workspace slot for DB-native materialization and engine work.

Enter this repo when:

- a task explicitly targets engine materialization work
- you need to confirm whether engine scope has been initialized yet

First files:

- `cadenza-engine/`
- [../../workspace-map.md](../../workspace-map.md)

Primary commands:

- No stable repo-local command surface is documented yet.

Contract role:

- Planned consumer of core, service, and DB contracts

Routing note:

- Confirm repo state before making assumptions. This directory is intentionally thin and may not yet function as a fully initialized child repo.
